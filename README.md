# SmartServer
Postup pri inštalácii:

    $ sudo apt-get install cmake
    $ cd /priecinok v ktorom su subory
    $ mkdir build
    $ cd build/
    $ cmake ../
    $ make -j4
    $ sudo make install
    $ ./smartServer
